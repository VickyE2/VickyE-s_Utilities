name: Vicky-s_Utilities
version: '0.0.1-BETA'
main: org.vicky.vicky_utils
api-version: '1.20'
prefix: V-UTL
authors: [ VickyE2 ]
description: A plugin For all My essentials
dependencies:
  server:
    PlaceholderAPI:
      load: AFTER
      required: true
    MythicMobs:
      load: AFTER
      required: true
    ItemsAdder:
      load: AFTER
      required: false
    LoneLibs:
      load: AFTER
      required: false
    LuckPerms:
      load: AFTER
      required: true
    ProtocolLib:
      load: AFTER
      required: true