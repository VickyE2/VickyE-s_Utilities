name: Vicky-s_Utilities
version: '0.0.1-BETA'
main: org.vicky.vicky_utils
load: STARTUP
api-version: '1.20'
prefix: V-UTL
authors: [ VickyE2 ]
description: A plugin For all My essentials
loader: org.vicky.VickyUtilsPluginLoader
dependencies:
  server:
    PlaceholderAPI:
      load: BEFORE
      required: true
    CommandAPI:
      load: BEFORE
      required: true
    MythicMobs:
      load: BEFORE
      required: true
    ItemsAdder:
      load: AFTER
      required: true
    LoneLibs:
      load: BEFORE
      required: false
    WorldGuard:
      load: BEFORE
      required: false
    LuckPerms:
      load: BEFORE
      required: true
    ProtocolLib:
      load: BEFORE
      required: true
    BetterHud:
      required: true
      join-classpath: true
      load: BEFORE